meta {
  name: Sign in
  type: http
  seq: 3
}

get {
  url: {{server_url}}/api/v1/user/sign-in
  body: json
  auth: inherit
}

body:json {
  {
    "email": "example@email.be",
    "password": "verystrongpassword"
  }
}
